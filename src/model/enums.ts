export enum relationshipType{
    parent,
    child,
    spouse
}

export enum relationshipStatus{
    married,
    divorced,
    adopted,
    natural
}

export enum gender {
    male,
    female,
    x
}